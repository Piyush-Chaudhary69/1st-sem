#include<stdio.h>
#include<string.h>
struct std_info{
	int id;
	char name[20];
	long long int ph; 
};

int main()
{
	struct std_info deepak;
	printf("\nenter id: ");
	scanf("%d",&deepak.id);
	fflush(stdin);
	printf("\n Enter name: ");
	gets()
}